# Code examples

> Keep in mind, all of these code samples are untested as of `14/01/2026`

Both of these files assume that a `.env` file exists with `SCHOOL_NAME` and `AUTH_TOKEN` inside. Read how to obtain a token in [`/docs/auth`](/docs/auth)

## `classes.ts`

This file shows an example of how you would get upcoming classes.

## `grades.ts`

This file shows an example of how you would get the latest grades
